/**
 * API 応答に付ける共通のヘッダー。
 *
 * `cache-control: no-store` は**ログイン中の本人にだけ意味がある応答**（残り回数・契約状況・
 * 店舗一覧など）に必ず付ける。付け忘れると Vercel の CDN やブラウザが応答を持ち回り、
 * 別の利用者に前の人の内容が出る事故につながる。
 *
 * 2026-09-18 まで、まったく同じ定義が 18 ファイルに重複していた。値を 1 か所に置く。
 */

/** 保存させない（利用者ごとに変わる応答に付ける） */
export const NO_STORE = { "cache-control": "no-store" } as const;
