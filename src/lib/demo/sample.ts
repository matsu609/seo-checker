/**
 * 「計測前のイメージ」（破線の見本）の共通の決めごと。
 *
 * AI 検索モニタリング・順位計測・MEO でそれぞれ同じ値を持っていたので 1 か所にまとめた（2026-09-23）。
 * 見本の横軸（これからの日付）は `./dates` の comingWeekdays。
 */

/** 見本に描く点の数（＝これからの計測回数。AI 検索モニタリングでは週の数） */
export const SAMPLE_POINTS = 4;

/** 見本に描く線の本数の上限（多いと図が読めない） */
export const SAMPLE_SERIES_MAX = 3;
