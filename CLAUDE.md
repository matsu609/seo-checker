@AGENTS.md

# 引き継ぎメモの更新（必須・利用者の指示）

- **利用者とのやり取りのたびに、作業の最後に `docs/dev/OPERATIONS.md` を更新して main に push する。**このファイルだけで引き継ぎが完了する状態を保つ（現在の状態・残タスク・入力待ち・判断の経緯・作業ログ）。
- メモの更新だけなら作業ブランチ・検証・`add-release.mjs` は不要。main に直接 `運用メモを更新（YYYY-MM-DD）` でコミットする。コードを触ったときは従来どおり（ブランチ → lint / tsc / test / build → マージ → `add-release.mjs`）。
- 秘密の値（`sk_`、`GOCSPX-`、API キー）はメモに書かない。変数名と設定済み / 未設定だけを書く。
- セッションの最初に `docs/dev/OPERATIONS.md` の「再開の手順」を読んでから始める。
- 日本語でやり取りする。
