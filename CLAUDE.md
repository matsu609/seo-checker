@AGENTS.md

# 返答フォーマット（必須）

Claude Code の返答は、**毎回いちばん最後に**「GitHubのマージ状態」ブロックを付けて返すこと。
本文（作業内容の説明）を書いたあと、区切りとしてこのブロックを置く。

## 出力テンプレート

**GitHubのマージ状態**

- `main` へのマージ: マージ済み / 未マージ（PR `#<番号>`、マージコミット `<短縮SHA>`）
- 作業ブランチ: `<ブランチ名>`（リモートへプッシュ済み / 未プッシュ、未コミットの変更 あり / なし）
- プルリクエスト: `#<番号>` ・ `#<番号>` すべてマージ済み（`<PRのURL>`）
- `main` に対して先行 `<N>` コミット・遅れ `<N>` コミット、コンフリクト あり / なし
- リポジトリ内でオープンのままの他のプルリクエスト: あり（`#<番号>`）/ なし

続けて、コミットグラフをコードブロックで示す:

```
main            ─── M#138 ─────── M#139 (371b835) ─── M#140 (819415e) ─── M#141 (64bec8f)   ← origin/main
（最新）
                       \        /                  /                   /
branch                  └ 610a165 ─ 0c3db66 ─ be8bd2b ─────── 7231378 ┘                     ← 作業ブランチ
（プッシュ済み・全てマージ済み）
```

## ルール

- 値は必ず実際の `git` / GitHub の状態から取得する（`git status -sb`、`git log --oneline --graph`、
  `git rev-list --left-right --count origin/main...HEAD`、PR 情報は GitHub MCP ツール）。推測で書かない。
- 取得できない項目は `不明` と書く。ブランチ名・SHA・PR 番号を捏造しない。
- ブランチが `main` そのもの、または PR が存在しない場合も、該当項目を「なし」としてブロック自体は必ず出す。
- git リポジトリ外で作業している場合のみ、ブロックの代わりに「git リポジトリではありません」と 1 行で書く。
