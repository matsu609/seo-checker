@AGENTS.md

# 引き継ぎメモの更新（必須・利用者の指示）

- **利用者とのやり取りのたびに、作業の最後に `docs/dev/OPERATIONS.md` を更新して main に push する。**このファイルだけで引き継ぎが完了する状態を保つ（現在の状態・残タスク・入力待ち・判断の経緯・作業ログ）。
- メモの更新だけなら作業ブランチ・検証・`add-release.mjs` は不要。main に直接 `運用メモを更新（YYYY-MM-DD）` でコミットする。コードを触ったときは従来どおり（ブランチ → lint / tsc / test / build → マージ → `add-release.mjs`）。
- 秘密の値（`sk_`、`GOCSPX-`、API キー）はメモに書かない。変数名と設定済み / 未設定だけを書く。
- セッションの最初に `docs/dev/OPERATIONS.md` の「再開の手順」を読んでから始める。
- 日本語でやり取りする。
- 利用者に作業をお願いするときは、手順ごとに**どのサービスの、どの画面か（URL つき）**を必ず書く（利用者の指示 2026-09-10）。表の形（# / サービス・画面 / URL / やること）にする。

# 返答フォーマット: 合流の図（必須・利用者の指示 2026-09-16）

**コミットを作った返答では、いちばん最後に「今回の作業がどう合流したか」の図を必ず付ける。**
利用者が「main に入ったのか」「push まで終わったのか」を、文章を読まずに一目で確かめられるようにするため。

## 1. まず「いまどこにいるか」

同じコミットを指しているものを 1 か所にまとめて描く。ずれているものは別の行に描く。

```
                                    ┌─ main                      ← ローカル
                                    ├─ origin/main               ← GitHub の本流
  <短縮SHA> ●━━━━━━━━━━━━━━━━━━━━━━━┤
    (最新)  │                       ├─ <作業ブランチ>              ← ローカル
            │                       └─ origin/<作業ブランチ>       ← GitHub
            │
            │   ぜんぶ同じ場所にいる = マージ漏れ・push 漏れなし
```

未マージや未 push があるときは、その状態をそのまま描く（「ぜんぶ同じ場所」と書かない）。例:

```
  ● <SHA>  ← origin/main（GitHub の本流）
  │
  │ ● <SHA>  ← 作業ブランチ（ローカルのみ・未 push・main に未マージ）
  ├─┘
```

## 2. つぎに「今回の作業がどう合流したか」

`git log --graph --oneline --decorate` の出力を、記号を `●` と罫線に置き換えて読みやすくしたもの。

- **自分が今回作ったコミットに `★` を付ける。**別セッションのコミットには付けない。
- 各行に「誰の作業か」が分かる短い説明を添える（`別セッション: …` など）。
- マージコミットには「マージ ①」「マージ ②」と番号を振り、競合を解決した場合はその旨を書く。
- 長い場合は今回の作業の起点までで切ってよい（全履歴を出さない）。

```
  ● <SHA>  ← いま全部がここ（main / 作業ブランチ / GitHub）
  ├─┐
  │ ● <SHA>  別セッション: <内容>
  ● │ <SHA>  マージ ②
  ├─┤
  │ ● <SHA>  別セッション: <内容>
  ● │ <SHA>  ★ <今回の作業>
  ├─┘  ← マージ ① （競合があればここに書く）
  ● <SHA>  ★ <今回の作業>
```

## 3. リリース番号を足した場合は一覧も出す

| 版 | 内容 | 誰 |
|---|---|---|
| rNN | … | 今回 / 別セッション |

## ルール

- **値は必ず実際の git の状態から取る。**`git fetch origin` → `git status -sb`、`git log --graph --oneline --decorate --all`、`git branch -vv --all`。**SHA・ブランチ名・リリース番号を推測で書かない。**
- 取れないものは `不明` と書く。捏造しない。
- コミットを作っていない返答（調査・相談だけ）では、この図は不要。
- 図のあとに、利用者にお願いしたい作業があれば上記の表（# / サービス・画面 / URL / やること）を続ける。
